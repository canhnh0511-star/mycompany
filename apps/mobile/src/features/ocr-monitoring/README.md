# features/ocr-monitoring

Màn "Theo dõi OCR" (Tuần 6, ADR-0019 mục 4) — đọc `ocr_call_logs` (list + `/stats`, backend có sẵn từ
Phase 4). Chỉ Admin xem (CLAUDE.md §2). Route: `(web)/reports/ocr-monitoring.tsx`, dùng chung rail layout
với `features/reports`.
