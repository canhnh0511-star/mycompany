import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function LatexTypesScreen() {
  return (
    <PlaceholderScreen
      title="Danh mục Loại mủ"
      description="Danh mục MỞ (water/cup/strip/coagulated hiện tại) — code không sửa được sau khi tạo, chỉ label/unit."
      reference="docs/adr/0002-normalize-latex-type-storage.md, services/api LatexTypeController"
    />
  );
}
