import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function LookupScreen() {
  return (
    <PlaceholderScreen
      title="Tra cứu"
      description="Lọc theo Tổ/nhân viên/khoảng ngày/trạng thái (kể cả draft), xem chi tiết record + ảnh gốc + lịch sử chỉnh sửa."
      reference="docs/adr/0012-ocr-review-table-data-source.md, features/production-records/ + features/latex-sales/"
    />
  );
}
