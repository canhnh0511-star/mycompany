# Copy sang .env.local (đã gitignore) khi chạy dev. Biến EXPO_PUBLIC_* được inline lúc build
# (https://docs.expo.dev/guides/environment-variables/) — apiClient (src/lib/api/client.ts) đọc biến này.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
