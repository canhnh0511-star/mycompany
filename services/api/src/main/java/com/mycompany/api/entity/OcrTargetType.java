package com.mycompany.api.entity;

/** Loại phiếu Admin ĐÃ CHỌN trước khi chụp/chọn ảnh (không phải loại thực tế đọc được từ ảnh). */
public enum OcrTargetType {
    PRODUCTION_RECORD,
    LATEX_SALE
}
